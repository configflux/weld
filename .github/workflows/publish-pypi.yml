# Release workflow for weld (public repo)
# DIAGNOSTIC VERSION — minimized to isolate startup_failure

name: Publish to PyPI

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:
    inputs:
      version:
        description: "Version tag to publish (e.g. v0.5.0)"
        required: true
        type: string

permissions:
  contents: read

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Publish to PyPI - diagnostic minimal body"
