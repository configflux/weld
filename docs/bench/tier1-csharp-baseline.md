# C# Tier-1 baseline -- snapshot

Generated 2026-05-16, workspace at `8ce7e6b` (weld 0.19.1).

This document is the frozen reference for the post-promotion C# Tier-1
state. It records the per-criterion outcome of `tools/tier_check.py`
against each of the three pinned corpora declared in
[`tier1-corpora.md`](tier1-corpora.md). It is intentionally
diff-friendly: a future regression to any of the structural facts
below (kind correctness, edge counts, F1 scores, determinism SHA-256,
performance LOC, framework strategies) will show as a line-level
change in this file.

The numbers were generated by running `python3 -m weld discover`
against each pinned corpus at workspace HEAD, followed by
`python3 tools/tier_check.py --language csharp --corpus <corpus>
--rediscover --json`. See "How to refresh" at the bottom.

## Summary

All three pinned C# corpora report `verdict='tier1'` at this workspace
SHA. Per the ADR 0064 amendment, criterion 6 (`description_coverage`)
is advisory -- it is measured and reported but does not gate
promotion -- and is currently 0.0% on ShareX and Newtonsoft.Json
(neither has been run through an enrichment provider) and 2.35% on
eShopOnWeb (carried over from a partial enrichment pass).

| Corpus              | LOC     | Verdict | C1 kind | C2 edges | C3 framework | C4 determinism | C5 federation | C6 desc | C7 F1 | C8 perf |
|---------------------|---------|---------|---------|----------|--------------|----------------|---------------|---------|-------|---------|
| ShareX              | 240 756 | tier1   | pass    | pass     | stub         | pass           | stub          | fail    | pass  | pass    |
| eShopOnWeb          | 10 733  | tier1   | pass    | pass     | pass         | pass           | stub          | fail    | pass  | pass    |
| Newtonsoft.Json     | 193 410 | tier1   | pass    | pass     | pass         | pass           | stub          | fail    | pass  | pass    |

The `stub` outcomes are stub-by-design (acceptable-for-tier1=true):
none of the three corpora are polyrepo federation roots (C5) and only
eShopOnWeb / Newtonsoft.Json carry the framework markers that activate
C3. The `fail` on C6 is the advisory state described above; under the
post-mk9r verdict ladder it does not block promotion.

## ShareX (desktop monorepo)

- Source: <https://github.com/ShareX/ShareX>
- Corpus SHA: `42465d3e9bee52c780c0a905c639b9fb0a958a00`
- Workspace SHA: `8ce7e6b2607ee6e84521cb51b3dd0bb8981769e0`
- Verdict: `tier1`

| Criterion                  | Status | Key detail                                                                                                                  |
|----------------------------|--------|-----------------------------------------------------------------------------------------------------------------------------|
| C1 kind_correctness        | pass   | bad_count=0; total_symbols=15 680; vocabulary={class, controller, dbcontext, enum, interface, method, property, record, struct} |
| C2 class_level_edges       | pass   | symbol_origin_pct=100.0%; symbol_origin_count=890; threshold=99.0%; total_edges=890                                          |
| C3 framework_strategies    | stub   | acceptable_for_tier1=true; markers_present=[]; reason="no framework markers found in corpus" (classic Windows desktop app)    |
| C4 determinism             | pass   | sha256=`8c52eb6489b70590dd06f95efaf25204884ad9b0d49a7058cbd5725fa75bca93` (identical across two `wd discover` runs)           |
| C5 federation              | stub   | acceptable_for_tier1=true; repo_node_count=0; reason="corpus is not a polyrepo workspace root"                                |
| C6 description_coverage    | fail   | ADVISORY only; coverage_pct=0.0%; threshold=70.0%; total_meaningful_symbols=15 680; no enrichment pass run                    |
| C7 query_f1                | pass   | mean_f1=1.0; threshold=0.9; entry_count=21 (5 query, 5 inherits_from, 2 implements, 2 callers_of, 7 contained_by; all 1.0)    |
| C8 performance             | pass   | loc=240 756; seconds_per_100k_loc=10.08; budget=60 s/100k; wall_clock (first observed)=24.26 s                                |

## eShopOnWeb (framework monorepo)

- Source: <https://github.com/dotnet-architecture/eShopOnWeb>
- Corpus SHA: `4da8212117e87d808d4bbc7da6286fd2147ce606`
- Workspace SHA: `8ce7e6b2607ee6e84521cb51b3dd0bb8981769e0`
- Verdict: `tier1`

| Criterion                  | Status | Key detail                                                                                                                                                                            |
|----------------------------|--------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| C1 kind_correctness        | pass   | bad_count=0; total_symbols=899; vocabulary={class, controller, dbcontext, enum, interface, method, property, record, struct}                                                          |
| C2 class_level_edges       | pass   | symbol_origin_pct=100.0%; symbol_origin_count=142; threshold=99.0%; total_edges=142                                                                                                    |
| C3 framework_strategies    | pass   | markers_present=[`csharp_aspnet_routes`, `csharp_efcore`, `csharp_test_framework`]; each marker resolved (AspNetCore route table, EF DbContext set, xUnit test suites)                  |
| C4 determinism             | pass   | sha256=`207dc3471a1d0fd0d89726c04b0032962e532f8f55dd6c5bb7bdbf396670246a` (identical across two `wd discover` runs)                                                                    |
| C5 federation              | stub   | acceptable_for_tier1=true; repo_node_count=0; reason="corpus is not a polyrepo workspace root"                                                                                         |
| C6 description_coverage    | fail   | ADVISORY only; coverage_pct=2.35%; threshold=70.0%; total_meaningful_symbols=895; small carry-over enrichment, not a full pass                                                          |
| C7 query_f1                | pass   | mean_f1=0.9929; threshold=0.9; entry_count=28 (10 query, 6 inherits_from, 5 implements, 3 callers_of, 4 contained_by); worst case `query-basket-service` at 0.80, all others 1.00      |
| C8 performance             | pass   | loc=10 733; seconds_per_100k_loc=20.94; budget=60 s/100k; wall_clock (first observed)=2.25 s                                                                                            |

## Newtonsoft.Json (library monorepo)

- Source: <https://github.com/JamesNK/Newtonsoft.Json>
- Corpus SHA: `4f73e74372445108d2c1bda37b36e6f5e43402e0`
- Workspace SHA: `8ce7e6b2607ee6e84521cb51b3dd0bb8981769e0`
- Verdict: `tier1`

| Criterion                  | Status | Key detail                                                                                                                                          |
|----------------------------|--------|-----------------------------------------------------------------------------------------------------------------------------------------------------|
| C1 kind_correctness        | pass   | bad_count=0; total_symbols=9 749; vocabulary={class, controller, dbcontext, enum, interface, method, property, record, struct}                       |
| C2 class_level_edges       | pass   | symbol_origin_pct=100.0%; symbol_origin_count=874; threshold=99.0%; total_edges=874                                                                  |
| C3 framework_strategies    | pass   | markers_present=[`csharp_test_framework`]; only the test-framework strategy resolves (xUnit suites); ASP.NET / EF / MSBuild markers are absent (library, not a service) |
| C4 determinism             | pass   | sha256=`057c76bcd6318158c202fdc0ee64694d8cc6e38a93e8694bc3865093dcbd21d5` (identical across two `wd discover` runs)                                   |
| C5 federation              | stub   | acceptable_for_tier1=true; repo_node_count=0; reason="corpus is not a polyrepo workspace root"                                                       |
| C6 description_coverage    | fail   | ADVISORY only; coverage_pct=0.0%; threshold=70.0%; total_meaningful_symbols=9 749; no enrichment pass run                                            |
| C7 query_f1                | pass   | mean_f1=1.0; threshold=0.9; entry_count=10 (4 query, 4 inherits_from, 2 implements; all 1.0)                                                          |
| C8 performance             | pass   | loc=193 410; seconds_per_100k_loc=8.48; budget=60 s/100k; wall_clock (first observed)=16.40 s                                                         |

## Other languages (preview, measured)

Python / C++ / Java are not Tier-1. They are run against the bundled
in-repo fixtures only (no external corpora pinned). The harness
reports `verdict='preview'` on each, which exercises the verdict
ladder's "do not over-promote" path and confirms that
language-agnostic criteria (C1, C4) report coherently while
language-specific scoring (C3, C7) correctly signals
not-yet-Tier-1.

| Fixture                                                | Verdict | C1   | C2   | C3   | C4   | C5   | C6   | C7   | C8   |
|--------------------------------------------------------|---------|------|------|------|------|------|------|------|------|
| `weld/tests/fixtures/tier1/python/sample_python`       | preview | pass | stub | stub | pass | stub | stub | fail | fail |
| `weld/tests/fixtures/tier1/cpp/sample_cpp`             | preview | pass | stub | stub | pass | stub | stub | fail | fail |
| `weld/tests/fixtures/tier1/java/sample_java`           | preview | pass | stub | stub | pass | stub | stub | fail | fail |

Notes on the preview rows:

- C7 `fail` reflects the bundled fixture sample sizes (5-6 queries on
  ~80 lines of code) where the fixture queries do not yet have
  matching gold sets; this is expected at preview and will be
  reassessed when each language enters its Tier-1 promotion epic.
- C8 `fail` is fixture-noise: a 70-100 LOC sample inflates
  seconds_per_100k_loc because the harness amortizes fixed
  startup cost over too little code. Tier-1 promotion measures
  C8 against the pinned external corpora, not bundled fixtures.
- The point of running the preview rows here is exactly to confirm
  the harness produces a coherent envelope (no crashes) for
  not-yet-Tier-1 languages, which it does.

## How to refresh

The doc above is reproducible from the workspace HEAD plus the three
pinned corpora. To regenerate:

```bash
# 1. Materialise the corpora at their pinned SHAs (idempotent).
python3 tools/tier1_corpus.py --language csharp --fetch \
    --output /tmp/weld-tier1-corpora

# 2. Re-run tier_check against each. --rediscover ensures the corpus
#    graph is built with workspace-HEAD weld (not a possibly-lagging
#    installed wd binary). --json captures the full criterion payload.
for corpus in ShareX eShopOnWeb Newtonsoft.Json; do
    PYTHONPATH=. python3 tools/tier_check.py \
        --language csharp \
        --corpus /tmp/weld-tier1-corpora/$corpus \
        --rediscover \
        --json > /tmp/tc-$(echo $corpus | tr '[:upper:]' '[:lower:]').json
done

# 3. Inspect verdicts and per-criterion status.
for f in /tmp/tc-sharex.json /tmp/tc-eshoponweb.json /tmp/tc-newtonsoft.json; do
    python3 -c "import json,sys; d=json.load(open(sys.argv[1])); \
        print(d['corpus'], '->', d['verdict']); \
        [print(' ',k,v['status']) for k,v in sorted(d['criteria'].items())]" \
        "$f"
done
```

Workspace SHA, corpus SHAs, kind vocabulary, edge counts, F1
scores, determinism SHA-256s and LOC counts are deterministic and
should reproduce byte-identically. The `wall_clock` values in the
performance criterion are wall time and will vary by host -- the
`seconds_per_100k_loc` column is bound by the 60 s/100k-LOC budget
declared in ADR 0064 and is the field that matters for regression
detection.

If any structural fact above changes against the same workspace SHA
and corpus SHA pair, that is a real regression and should be filed
as a bd issue against the C# Tier-1 epic.

## Related

- [ADR 0064](../adrs/0064-tier-1-language-support-contract.md) --
  the Tier-1 criteria contract (criterion 6 reclassified advisory in
  the 2026-05-16 amendment).
- [`tier1-corpora.md`](tier1-corpora.md) -- the pinned corpus
  manifest these SHAs come from.
- [`tools/tier_check.py`](../../tools/tier_check.py) -- the harness
  whose output is captured here.
